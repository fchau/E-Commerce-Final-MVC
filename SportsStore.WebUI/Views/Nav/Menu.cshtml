@model IEnumerable<string>

@foreach (var link in Model)
{
    @Html.RouteLink(link, new{
    controller = "Product",
    action = "List",
    category = link,
    page = 1
},
  new {
      @class = link == ViewData["SelectedCategory"] ? "selected" : null
  })
  
}